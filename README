此工程是按照视频来编写的外卖点餐程序，点餐程序分为四个服务提供者和1个服务调用者，四个服务提供者分别是：
1、menu用于读取菜品信息，实现菜品的增删改查操作，有前端页面，可访问localhost:9003/menu/redirect/index进行访问。
2、user用于管理和添加用户
3、account用于进行用户和管理员的登陆认证，此服务会同时访问user和admin两个数据表，返回相应的信息，
4、order是处理用户的订单信息的。
服务调用者：client用于访问四个服务提供者，四个服务提供者所需的调用接口都已经实现，具体的页面跳转和前端操作处理都在client中，menu的所有页面都已经实现，可添加、删除、修改菜品信息。user用于处理用户信息的网页。account的页面还没有实现。
